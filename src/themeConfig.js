import { createTheme } from "@mui/material/styles";

let customTheme = createTheme({
  palette: {
    primary: {
      /* main: "#0052cc", */
      main: "#000000",
    },
    secondary: {
      main: "#0052cc",
    },
  },
});

export default customTheme;
